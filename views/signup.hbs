<!DOCTYPE html>

<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta charset="utf-8" />
    <title>Bids to Pick - Create an Account</title>

    <style>
        @import "https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css";

        body {
            margin: 0;
            padding: 0;
            font-family: sans-serif;
            background: url(/images/bg2.jpg) no-repeat;
            background-size: cover;
        }

        #container {
            width: 30%;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: rgb(60,179,113);
            background: rgba(232,232,232);
        }

            #container h1 {
                font-size: 40px;
                border-bottom: 6px solid rgb(60,179,113);
                margin-bottom: 50px;
                padding: 13px 0;
            }
		
        #textbox1 {
            width: 100%;
            overflow: hidden;
            font-size: 20px;
            padding: 8px 0;
            margin: 8px 0;
            border-bottom: 1px solid #474540;
        }

        #textbox2 {
            width: 100%;
            overflow: hidden;
            font-size: 20px;
            padding: 8px 0;
            margin: 8px 0;
            border-bottom: 1px solid #474540;
        }

        #textbox3 {
            width: 100%;
            overflow: hidden;
            font-size: 20px;
            padding: 8px 0;
            margin: 8px 0;
            border-bottom: 1px solid #474540;
        }

        #textbox4 {
            width: 100%;
            overflow: hidden;
            font-size: 20px;
            padding: 8px 0;
            margin: 8px 0;
            border-bottom: 1px solid #474540;
        }

        #textbox5 {
            width: 100%;
            overflow: hidden;
            font-size: 20px;
            padding: 8px 0;
            margin: 8px 0;
            border-bottom: 1px solid #474540;
        }

        .textbox i {
            width: 26px;
            float: left;
            text-align: center;
        }

        .textbox input {
            border: none;
            outline: none;
            background: none;
            color: white;
            font-size: 18px;
            width: 80%;
            float: left;
            margin: 0 10px;
        }

        #submit {
            width: 60%;
            background: white;
            border: 1px solid #474540;
            color: #292826;
            padding: 10px;
            font-size: 18px;
            cursor: pointer;
            margin-top: 40px;
            margin-left: 20%;
            margin-right: 20%;
        }
		
        #loginbtn{
            border-bottom: 1px solid black;
            color:black;
        }

		.login{
             text-align: center;
             color: black;
        }
		
		a:link {
		  text-decoration: none;
		  color: white;
		}

	    a:visited {
		      text-decoration: none;
		      color: white;
		    }

	    a:hover {
		      text-decoration: underline;
		      color: white;
		    }

	    a:active {
		      text-decoration: underline;
		      color: white;
		    }
	
		::placeholder{
            color: black;
        }
		
		::placeholder{
            color: black;
        }

    </style>

    <script src="https://code.jquery.com/jquery-3.4.1.min.js" type="text/javascript"></script>


</head>
<body>	

	<script type="text/javascript">

        $(document).ready(function () {

            
            function checkField(field, val) {
            var valid = val;

            if(field.val() == '') {
                valid = false;
                field.css('border-color', 'red');
                $("#warning").text("Fill-in missing information.");
            } else {
                field.css('border-color', 'white');
            }

            return valid;
        }

        function checkEqual(field1, field2, val) {
            var valid = val;

            if(field1.val() != field2.val() || field1.val() == '') {
                valid = false;
                $("#textbox3").css('border-color', 'red');
				$("#textbox4")
                $("#warning").text("Fill-in missing information.");
            } else {
                field1.css('border-color', 'white');
                field2.css('border-color', 'white');
            }

            return valid;
        }

        function checkEmail(field, val) {
            var valid = val;
            var atIndex = field.val().indexOf('@');
            var dotIndex = field.val().lastIndexOf('.');

            if(atIndex == -1 || dotIndex == -1 || atIndex > dotIndex) {
                valid = false;
                $("#textbox2").css('border-color', 'red');
                $("#warning").text("Wrong Email Format");
            } else {
                field.css('border-color', 'white');
            }

            return valid;
        }


            $("#submit").click(function () {

                var valid = true;
				valid = checkField($("#fname"), valid);
				valid = checkField($("#lname"), valid);
				valid = checkField($("#email"), valid);
				valid = checkField($("#pw"), valid);
				valid = checkField($("#cpw"), valid);
                valid = checkField($("#pnumber"), valid);

                valid = checkEmail($("#email"), valid);

				valid = checkEqual($("#pw"), $("#cpw"), valid);
                
             });

        });

    </script>

<section id="container">
	<h1 id="title" style="margin-top: 2px; text-align:center; color:black;"> Bids To Pick <br> Sign Up </h1>
    <form id="signup" method="post">
		<div class="textbox" id="textbox1">
			<i class="fa fa-user" aria-hidden="true"></i>
			<input type="text" placeholder="Full Name" name="fname" id="fname" style="color:black" required>
		</div>

		<div class="textbox" id="textbox2">
			<i class="fa fa-envelope" aria-hidden="true"></i>
			<input type="text" placeholder="Email" name="email" id="email" style="color:black" required>
		</div>

		<div class="textbox" id="textbox3">
			<i class="fa fa-lock" aria-hidden="true"></i>
			<input type="password" placeholder="Password" name="pw" id="pw" style="color:black" required>
		</div>

		<div class="textbox" id="textbox4">
			<i class="fa fa-lock" aria-hidden="true"></i>
			<input type="password" placeholder="Confirm Password" name="cpw" id="cpw" style="color:black" required>
		</div>

		<div class="textbox" id="textbox5">
			<i class="fa fa-mobile" aria-hidden="true"></i>
			<input type="text" placeholder="Phone Number" name="pnumber" id="pnumber" style="color:black" required>
		</div>
		<input type="submit" id="submit" value="SUBMIT">
    </form>
	<div class="login">
            <p>Have an account?</p> <a id="loginbtn" href="login" target="_self" title="Login Information">Log In</a><hr>
	</div>
</section>>

</body>
</html>
